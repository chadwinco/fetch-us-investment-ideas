interface:
  display_name: "Fetch US Investment Ideas"
  short_description: "Generate US ticker ideas with short value-quality theses"
  default_prompt: "Use $fetch-us-investment-ideas as an LLM-driven workflow: use native web research for flexible sourcing, the local Finviz helper for bounded screening, and optional filing-seeded sourcing when local <DATA_ROOT>/daily-sec-filings JSONL snapshots already exist from external pipelines. Apply <DATA_ROOT>/user_preferences.json filters by default and append new tickers to per-screen queue files at <DATA_ROOT>/idea-screens/**/screener-results.jsonl."
  icon_small: "./assets/chadwin-icon.svg"
  icon_large: "./assets/chadwin-icon.png"
  brand_color: "#F43C00"

dependencies:
  python_packages:
    - "beautifulsoup4>=4.12"
